const initState = {
  postList: [],
};

function reducer(postState = initState, action) {
  switch (action.type) {
    case value:
      break;

    default:
      return postState;
  }
}
export default reducer;
